export const metadata = {
  title: 'ApexElement WhatsApp Webhook',
  description: 'WhatsApp webhook middleware for recipe service',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}